---
# vagrant commands (up, halt, destroy...)
- include_tasks:
    file: vagrant_by_commands_test.yml
    apply:
      tags:
        - by_commands
  tags:
    - by_commands
    - status
    - halt

# vagrant states (running, not_created, poweroff)
- include_tasks:
    file: vagrant_by_state_test.yml
    apply:
      tags:
        - by_states
  tags:
    - by_states
